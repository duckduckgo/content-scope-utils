{
  "name": "bundled-esbuild",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run build",
    "build": "esbuild --bundle src/messaging-consumer.js --outfile=dist/messaging-consumer.dist.js --sourcemap=inline",
    "build.watch": "npm run build -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.11",
    "@duckduckgo/content-scope-utils": "file:../../"
  }
}
